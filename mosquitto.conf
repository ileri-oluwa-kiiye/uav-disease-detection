# Standard MQTT (ESP32 connects here)
listener 1883
protocol mqtt

# MQTT over WebSocket (browser connects here)
listener 9001
protocol websockets

# Dev only — lock down with auth for production
allow_anonymous true
